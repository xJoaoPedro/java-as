package Tabuleiro.TiposDeCasa;

import Jogo.Jogador;
import Tabuleiro.Casa;

public class SorteReves extends Casa {
    // TODO objeto baralho

    public SorteReves() {
        super("sortereves", "Sorte / Revés", false);
    }

    @Override
    public void executarAcao(Jogador jogadorAtual) {
//        Implementar uma classe Baralho que gerencia uma Pilha (Stack) de objetos Carta.Criar um conjunto de, no
//        mínimo, 16 cartas com eventos variados (ex:ganhe / perca dinheiro, avance para o início, pague uma taxa
//        a todos, vá para a prisão, etc.).
//        Ao parar em uma casa de "Sorte/Revés", o jogador deve "puxar" uma carta do topo da pilha(pop).O efeito da
//        carta é aplicado imediatamente.
//        O sistema deve ter uma função para embaralhar as cartas no início do jogo, e quando a pilha de cartas se
//        esgotar, o sistema deve automaticamente embaralhar todas as cartas e recriar a pilha para que o jogo possa
//        continuar.
    }
}
